server:
  port: 8282

spring:


  datasource:
    #url: jdbc:postgresql://localhost:5432/products_gl
    url: jdbc:mysql://localhost:8000/produits?createDatabaseIfNotExist=true
    username: root
    password:


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    application:
      name: api-products

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
